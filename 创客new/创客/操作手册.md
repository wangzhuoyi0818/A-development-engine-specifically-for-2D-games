# 🎯 AutoDL 部署 - 5分钟完成指南

## ✅ 已完成的准备工作
- ✅ 项目已构建完成
- ✅ 压缩包已创建：`dist.tar.gz` (36MB)
- ✅ 部署脚本已准备好

---

## 📝 你只需要做 3 件事：

### 第 1️⃣ 步：上传压缩包到 AutoDL

1. **打开 AutoDL 的 JupyterLab**
   - 登录 https://www.autodl.com/console/instance/list
   - 找到你的实例
   - 点击"打开 JupyterLab"

2. **上传文件**
   - 在 JupyterLab 左侧文件浏览器
   - 确保在 `/root` 目录（默认就是）
   - 点击工具栏的"⬆️ 上传文件"按钮
   - 选择这个文件：
     ```
     E:\最新\最终融合版_20260127\创客new\创客\dist.tar.gz
     ```
   - 等待上传完成（36MB，大约 1-3 分钟）

---

### 第 2️⃣ 步：在终端运行命令

1. **打开终端**
   - 在 JupyterLab 顶部菜单：File → New → Terminal
   - 或者使用 SSH 连接（如果你熟悉）

2. **复制粘贴执行以下命令**（一次性全部复制）

```bash
# 🚀 一键部署命令（全自动）
curl -fsSL https://deb.nodesource.com/setup_20.x | bash - && \
apt install -y nodejs && \
npm install -g serve pm2 && \
mkdir -p /root/miniprogram && \
cd /root/miniprogram && \
tar -xzf /root/dist.tar.gz && \
pm2 delete miniprogram 2>/dev/null || true && \
pm2 serve /root/miniprogram 6006 --spa --name miniprogram && \
pm2 save && \
pm2 startup && \
echo "" && \
echo "✅ 部署完成！" && \
echo "📋 下一步：在 AutoDL 控制台配置端口映射（容器端口：6006，协议：HTTP）" && \
pm2 status
```

3. **等待执行完成**
   - 会自动安装 Node.js
   - 会自动解压文件
   - 会自动启动服务
   - 看到 "✅ 部署完成！" 就好了

---

### 第 3️⃣ 步：配置端口映射

1. **回到 AutoDL 控制台**
   - https://www.autodl.com/console/instance/list

2. **配置端口**
   - 找到你的实例
   - 点击"自定义服务"或"容器实例"
   - 点击"添加自定义服务"
   - 填写：
     ```
     容器端口：6006
     协议：HTTP
     ```
   - 点击"确定"

3. **获取访问地址**
   - 保存后会显示一个公网地址，例如：
     ```
     http://region-41.autodl.pro:12345
     ```

4. **访问你的网站**
   - 在浏览器输入上面的地址
   - 🎉 完成！

---

## 📱 测试功能

访问网站后测试：
- ✅ 添加精灵组件
- ✅ 拖拽虚拟摇杆到画布
- ✅ 进入预览模式
- ✅ 用鼠标拖动摇杆测试移动
- ✅ 按 WASD 键测试键盘控制

---

## 🔧 如果遇到问题

### 问题1：上传失败
- 检查网络连接
- 尝试使用 SSH 上传（需要提供 SSH 信息）

### 问题2：命令执行失败
在终端分步执行：
```bash
# 步骤1：安装环境
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs
npm install -g serve pm2

# 步骤2：解压文件
mkdir -p /root/miniprogram
cd /root/miniprogram
tar -xzf /root/dist.tar.gz

# 步骤3：启动服务
pm2 serve /root/miniprogram 6006 --spa --name miniprogram
pm2 save
```

### 问题3：端口被占用
```bash
# 更换端口（改用 7007）
pm2 delete miniprogram
pm2 serve /root/miniprogram 7007 --spa --name miniprogram
pm2 save

# 然后在 AutoDL 控制台映射 7007 端口
```

### 问题4：页面 404
```bash
# 检查文件
ls -la /root/miniprogram/index.html

# 查看日志
pm2 logs miniprogram
```

---

## 🎯 快速检查清单

部署前：
- [ ] 已打开 AutoDL JupyterLab
- [ ] 已准备好 dist.tar.gz 文件

部署中：
- [ ] 已上传 dist.tar.gz 到 /root/
- [ ] 已在终端执行部署命令
- [ ] 命令执行完成，显示 "✅ 部署完成！"

部署后：
- [ ] 已在 AutoDL 控制台配置端口映射（6006 → HTTP）
- [ ] 已获取公网访问地址
- [ ] 在浏览器可以正常访问

---

## 💡 后续操作

### 更新项目
```bash
# 1. 本地重新构建和压缩
npm run build
tar -czf dist.tar.gz -C dist .

# 2. 上传新的 dist.tar.gz 覆盖旧文件

# 3. 在 AutoDL 终端执行
cd /root/miniprogram
rm -rf *
tar -xzf /root/dist.tar.gz
pm2 restart miniprogram
```

### 查看状态
```bash
pm2 status
pm2 logs miniprogram
```

### 停止服务
```bash
pm2 stop miniprogram
```

---

## 📞 需要帮助？

如果遇到问题，提供以下信息：
1. 在哪一步遇到问题
2. 终端显示的错误信息
3. `pm2 logs miniprogram` 的输出

---

**现在开始第 1 步：上传 dist.tar.gz 到 AutoDL 的 JupyterLab！** 🚀
