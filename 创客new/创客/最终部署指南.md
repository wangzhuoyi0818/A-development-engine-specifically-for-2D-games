# 🎯 AutoDL 部署 - 最终版（3分钟完成）

## 📋 你的服务器信息

✅ **SSH连接：**
```
ssh -p 55567 root@connect.bjb1.seetacloud.com
密码: RYzudn9KCm27
```

✅ **已自动映射端口：**
- 容器端口 6006 → 公网地址：https://u812392-b4f4-d1b0eded.bjb1.seetacloud.com:8443
- 容器端口 6008 → 公网地址：https://uu812392-b4f4-d1b0eded.bjb1.seetacloud.com:8443

✅ **部署文件：**
- 压缩包位置：`E:\最新\最终融合版_20260127\创客new\创客\dist.tar.gz` (36MB)

---

## 🚀 三种部署方式（任选一种）

### 方式 1️⃣：JupyterLab（最简单，推荐）

#### 第一步：上传文件
1. 打开你的 AutoDL 实例的 JupyterLab（在控制台点击"打开JupyterLab"）
2. 在左侧文件浏览器，确保在 `/root` 目录
3. 点击工具栏的"⬆️ 上传"按钮
4. 选择文件：`E:\最新\最终融合版_20260127\创客new\创客\dist.tar.gz`
5. 等待上传完成（约1-3分钟）

#### 第二步：部署
1. 在 JupyterLab 顶部菜单：**File → New → Terminal**
2. 在终端中复制粘贴以下命令（一次性全部复制）：

```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | bash - && \
apt install -y nodejs && \
npm install -g serve pm2 && \
mkdir -p /root/miniprogram && \
cd /root/miniprogram && \
tar -xzf /root/dist.tar.gz && \
pm2 delete miniprogram 2>/dev/null || true && \
pm2 serve /root/miniprogram 6006 --spa --name miniprogram && \
pm2 save && \
pm2 startup && \
echo "" && \
echo "✅✅✅ 部署完成！✅✅✅" && \
echo "访问地址: https://u812392-b4f4-d1b0eded.bjb1.seetacloud.com:8443" && \
pm2 status
```

#### 第三步：访问
在浏览器打开：
```
https://u812392-b4f4-d1b0eded.bjb1.seetacloud.com:8443
```

---

### 方式 2️⃣：Windows 批处理脚本（自动化）

如果你安装了 **PuTTY** 工具：

1. 双击运行文件：`一键部署.bat`
2. 等待自动完成
3. 访问：https://u812392-b4f4-d1b0eded.bjb1.seetacloud.com:8443

---

### 方式 3️⃣：使用 SSH 命令行

如果你熟悉命令行，在 Windows PowerShell 或 Git Bash 中执行：

#### 上传文件：
```bash
cd "E:\最新\最终融合版_20260127\创客new\创客"
scp -P 55567 dist.tar.gz root@connect.bjb1.seetacloud.com:/root/
# 输入密码: RYzudn9KCm27
```

#### 连接并部署：
```bash
ssh -p 55567 root@connect.bjb1.seetacloud.com
# 输入密码: RYzudn9KCm27

# 然后在服务器上运行部署命令（见上面的命令）
```

---

## 🎉 部署完成后的测试

访问：https://u812392-b4f4-d1b0eded.bjb1.seetacloud.com:8443

测试功能：
1. ✅ 页面能正常加载
2. ✅ 添加精灵组件
3. ✅ 拖拽虚拟摇杆到画布
4. ✅ 进入预览模式（点击工具栏的 ▶️ 按钮）
5. ✅ 鼠标拖动摇杆测试移动
6. ✅ 按 WASD 键测试键盘控制
7. ✅ 在手机浏览器访问测试触摸摇杆

---

## 🔧 管理命令

连接到服务器后可以使用：

```bash
# 查看服务状态
pm2 status

# 查看实时日志
pm2 logs miniprogram

# 重启服务
pm2 restart miniprogram

# 停止服务
pm2 stop miniprogram

# 启动服务
pm2 start miniprogram
```

---

## 🐛 故障排除

### 问题1: 访问提示 502 Bad Gateway
```bash
# 检查服务是否运行
pm2 status

# 如果没有运行，启动服务
pm2 start miniprogram

# 或重新部署
cd /root/miniprogram
pm2 delete miniprogram
pm2 serve /root/miniprogram 6006 --spa --name miniprogram
pm2 save
```

### 问题2: 页面显示 404
```bash
# 检查文件是否存在
ls -la /root/miniprogram/index.html

# 如果文件不存在，重新解压
cd /root/miniprogram
rm -rf *
tar -xzf /root/dist.tar.gz
pm2 restart miniprogram
```

### 问题3: 端口冲突
```bash
# 查看端口占用
lsof -i :6006

# 停止占用进程
pm2 delete miniprogram

# 重新启动
pm2 serve /root/miniprogram 6006 --spa --name miniprogram
pm2 save
```

---

## 🔄 更新项目

当你修改了代码，需要重新部署：

```bash
# 1. 本地重新构建
npm run build
tar -czf dist.tar.gz -C dist .

# 2. 上传新的 dist.tar.gz（覆盖旧文件）

# 3. 在服务器上运行
cd /root/miniprogram
rm -rf *
tar -xzf /root/dist.tar.gz
pm2 restart miniprogram
```

---

## 📱 分享给朋友

你的游戏创作平台地址：
```
https://u812392-b4f4-d1b0eded.bjb1.seetacloud.com:8443
```

可以直接分享这个地址，任何人都可以访问！

支持：
- ✅ 电脑浏览器
- ✅ 手机浏览器
- ✅ 平板浏览器
- ✅ 触摸屏设备

---

## 🎯 现在就开始！

**最简单的方式：**
1. 打开 AutoDL JupyterLab
2. 上传 `dist.tar.gz`
3. 在终端运行一条命令
4. 访问网址

就这么简单！🚀

有任何问题随时告诉我！
